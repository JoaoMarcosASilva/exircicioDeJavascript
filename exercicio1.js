// 1 - Faça um algoritmo que leia os valores de A, B, C e em seguida imprima na tela a soma entre A e B é mostre se a soma é menor que C.

let A = 7
let B =1
let C = 9

let resultado = A + B

console.log(`A: ${A} + B: ${B} = ${resultado} e`, resultado > C ? `C: ${C} é maior do que o resultado ${resultado}` : `C: ${C} é maior do que o resultado da soma: ${resultado}`)
